# othello-ai

Agents are stored in `agent.py`

Game is ran using `main.py`

For testing game
`python3 main.py`
`python main.py`

More functions available later.

Install from requirements:
`pip3 install requirements.txt`
`pip install requirements.txt`